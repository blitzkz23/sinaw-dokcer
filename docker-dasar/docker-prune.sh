# hapus semua contaning yang tidak digunakan
docker container prune

# hapus semua image yang tidak digunakan
docker image prune

# hapus semua volume yang tidak digunakan
docker volume prune

# hapus semua network yang tidak digunakan
docker network prune

# hapus semua image, container, volume, dan network yang tidak digunakan
docker system prune
